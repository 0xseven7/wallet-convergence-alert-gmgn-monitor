# Project Handbook

Start here when another Codex thread needs project context.

## Monitor Extension

Read:

```text
docs/monitor-extension-handbook.md
```

This covers recent monitor-screen regressions and fixes:

- where to set the monitor screen;
- why the aggregate buy tray disappeared;
- why the extension icon stopped opening settings;
- why audio disappeared after refreshing GMGN;
- which files and regression tests guard those behaviors.

Important UI labels are written as Unicode escapes in the detailed handbook to
avoid Windows terminal encoding problems.

## Relay

Current official relay project work should default to:

```text
C:\Users\zx\work\relay
```

The relay folder under this repository is historical context unless the user
explicitly asks to edit this repo's legacy relay files.

