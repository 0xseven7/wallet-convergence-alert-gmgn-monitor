## v1.8.2 — 修 KOL/我的 列表重复钱包被算两次

**问题**：同一个钱包如果同时出现在 KOL 列表和我的列表里（比如你给加了备注名），由于两个频道用不同的显示名推送，dedup 判断不出是同一个，会被算成两个钱包。

**修复**（仅 xxyy）：
- dedup key 改用**钱包地址**（`walletAddr`），不再用显示名
- 同一钱包跨频道被推送时，把所有来源 merge 到 `sources` Set，不重复入池
- 我的面板的过滤条件改成 `sources.has('我的')`：即使该钱包被 KOL 频道也推过，只要也在我的列表里出现过，就算我的的
- 钱包来源标签优先显示「我的」（如果两边都在）
- 按钱包地址分组，同地址不同备注名只显示一条

副作用：从 DOM 扫描进来的数据没有钱包地址，仍然按显示名 dedup（行为不变）。socket.io 推送的有地址，享受新去重。
