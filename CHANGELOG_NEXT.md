## v1.7.1 — 修 gmgn 备注名行高被撑大

**问题**：gmgn 上有备注名的钱包，注入的 ☆ 按钮会跑到备注名上方，撑高整行 UI 扭曲。

**原因**：备注名的父容器是 `flex-col`，把 ☆ 插进去就跨成新行了。

**修复**：
- 注入时往上找最近的**横向 flex 容器**再插，避开 flex-col
- ☆ 的 CSS 改成 `inline-flex` + 固定 12×12 + `flex: 0 0 auto` 不被压缩，line-height: 1 不撑高
